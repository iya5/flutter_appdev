import 'package:flutter/material.dart';

class ColorPalette {
  // ---------- Backgrounds ----------
  static const Color background = Color(0xFF000000); // main black background
  static const Color appBar = Color(0xFF000000);

  // ---------- Text ----------
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color.fromARGB(197, 145, 145, 145);
  static const Color textSubtitle = Colors.white70;
  static const Color textMuted = Colors.white54;

  // ---------- Controls ----------
  static const Color sliderActive = Colors.white;
  static const Color sliderInactive = Color.fromARGB(198, 88, 88, 88);

  // ---------- Icons ----------
  static const Color iconPrimary = Colors.white;
  static const Color iconInactive = Color.fromARGB(197, 145, 145, 145);

  // ---------- Effects ----------
  static const Color splash = Colors.white;
  static const Color hoveredList = Color.fromARGB(29, 234, 234, 234);
}
